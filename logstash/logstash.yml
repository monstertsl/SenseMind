# =========================
# Logstash Configuration
# =========================

node.name: soc-logstash

# 多pipeline配置
pipeline.workers: 8
pipeline.batch.size: 500
pipeline.batch.delay: 50

# 持久化队列
queue.type: persisted
queue.max_bytes: 8gb

# 日志
log.level: info

# API
api.enabled: true
api.http.host: "0.0.0.0"

# 禁止自动加载默认pipeline（使用 pipelines.yml）
config.reload.automatic: false
